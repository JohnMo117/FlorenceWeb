# Changelog

## [Unreleased]
### Added
- Full project documentation set under `docs/`.
- Updated root `README.md` to reflect current architecture.

### Notes
- Backend routes currently mounted at:
  - `/api/auth`
  - `/api/admin`
  - `/api/teachers`
  - `/api/students`
- Frontend uses React + Vite in `client/`.
